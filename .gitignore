__pycache__/
*.pyc
*.pyo
.env
env/.env
.venv/
venv/

# Config + token rclone (upload Google Photos) — NE PAS committer
env/rclone.conf

src/archives/
rapport.csv

# Surcouche de config écrite par l'interface web — locale à la machine, jamais
# versionnée (c'est tout l'intérêt : 00-config.py ne bouge plus à chaque run).
00-config.local.py
**/00-config.local.py

# Cache de scan ffprobe (codec/dimensions par fichier) — généré, non versionné
.scan-cache.json
**/.scan-cache.json

# Cache d'identification des films (empreinte -> titre/année) —
# généré par 03-identify-movies.py, non versionné
.identify-cache.json
**/.identify-cache.json

# Cache de sondage audio (débit réel par piste) — généré par 04-slim-audio.py
.audio-cache.json
**/.audio-cache.json
*.tmp

# Fichiers log de conversion — générés, non versionnés
conversion.log
dry_run.log
**/conversion.log
**/dry_run.log
# …y compris les sorties redirigées à la main (gui.log, audio-run.log, etc.)
*.log

# Dossier de sortie de compress-for-gphotos.py (copies compressées)
output/

# Worktrees Claude Code — locaux, non versionnés
.claude/worktrees/
